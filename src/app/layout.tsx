import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-br">
      <body className={`ntialiased`}>
        <div className="flex flex-row w-full min-h-full">
          <aside className="w-[20vw] bg-[#1f2937]">
            <nav className="m-10">
              <ul className="list-none flex flex-col gap-14 items-center">
                <li>
                  <a
                    className="no-underline text-[1.8rem] font-bold text-white"
                    href="/"
                  >
                    📊 Dashboard
                  </a>
                </li>
                <li>
                  <a
                    className="no-underline text-[1.8rem] font-bold text-white"
                    href="new-transaction"
                  >
                    ➕ Nova Transação
                  </a>
                </li>
              </ul>
            </nav>
          </aside>
          <div className="w-dvw min-h-[100vh] p-[4.5rem] bg-[#f9fafb]">
            {children}
          </div>
        </div>
      </body>
    </html>
  );
}
